<html>
<head>
    <title>Main</title>
</head>
<body>
    <h1>Hello, 
        {% if user.email %}
            {{ user.email }}
        {% else %}
            dear guest!
        {% endif %}
    </h1>

    {% if not user.email %}
        <a href="/login">Login Page</a>
    {% endif %}
</body>
</html>