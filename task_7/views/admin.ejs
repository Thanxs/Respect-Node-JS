<div class="admin">
    Only admins can get access to this page
</div>

<div class="users-list">        
    <% if(typeof users != 'undefined') { %>            
        <% users.forEach(function(user) { %>
          <div class="user-item">
            <%= user %>
          </div>
        <% }); %>            
      <% } %>
  </div>